spring:
  application:
    name: horizont
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080

finnhub:
  api:
    url: https://finnhub.io/api/v1
    key: d09tp49r01qus8rfcsb0d09tp49r01qus8rf
